/** Full-width content with responsive horizontal insets (mobile-first). */
export const PAGE_INSET =
  "w-full min-w-0 max-w-none px-4 sm:px-6 md:px-10 lg:px-14 xl:px-20 2xl:px-28";

/** Main column: clears fixed mobile tab bar + iOS home indicator. */
export const MAIN_PAD =
  "pt-8 pb-[max(6.5rem,calc(5.5rem+env(safe-area-inset-bottom)))] sm:pt-10 md:pb-12 md:pt-12 lg:pb-16 lg:pt-16";
